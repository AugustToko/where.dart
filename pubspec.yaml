name: where
version: 4.0.1

author: Cédric Belin <cedric@belin.io>
description: Find the instances of an executable in the system path.
documentation: https://cedx.github.io/where.dart/api
homepage: https://cedx.github.io/where.dart

environment:
  sdk: '>=2.0.0-dev <2.0.0'

executables:
  where: where

dependencies:
  args: ^1.4.2
  file: ^5.0.0
  nodejs_interop: ^0.10.0
  platform: ^2.1.2
  process: ^3.0.1
  yaml: ^2.1.13

dependency_overrides:
  nodejs_interop:
    path: ../nodejs-interop.dart

dev_dependencies:
  coverage: ^0.11.0
  grinder: ^0.8.1
  node_preamble: ^1.4.0
  test: ^0.12.34
