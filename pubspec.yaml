name: where
version: 4.4.0

author: Cédric Belin <cedric@belin.io>
description: Find the instances of an executable in the system path. Like the `which` Linux command.
documentation: https://dev.belin.io/where.dart/api
homepage: https://dev.belin.io/where.dart

environment:
  sdk: '>=2.0.0 <3.0.0'

executables:
  where: where

dependencies:
  build_cli_annotations: ^1.0.0
  path: ^1.6.2

dev_dependencies:
  build_cli: ^1.0.1
  build_runner: ^1.0.0
  build_test: ^0.10.3
  build_version: ^2.0.0
  coverage: ^0.12.2
  grinder: ^0.8.3
  test: ^1.3.4
