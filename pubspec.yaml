name: where
version: 3.1.0

author: Cédric Belin <cedric@belin.io>
description: Find the instances of an executable in the system path.
documentation: https://cedx.github.io/where.dart
homepage: https://github.com/cedx/where.dart

environment:
  sdk: '>=1.24.0 <2.0.0'

executables:
  where: where

dependencies:
  args: ^1.2.0
  file: ^2.3.5
  nodejs_interop: ^0.8.0
  platform: ^2.1.1
  process: ^2.0.7
  yaml: ^2.1.13

dependency_overrides:
  nodejs_interop:
    path: ../nodejs-interop.dart

dev_dependencies:
  coverage: ^0.10.0
  grinder: ^0.8.1
  node_preamble: ^1.4.0
  test: ^0.12.30
